# Build-matrix configurations that do NOT compile today.
#
# tools/eval/build_matrix.sh fails on anything broken that is not listed here,
# and on anything listed here that has started building — so this file can only
# shrink, and only on purpose.
#
# Currently empty. It held moddwarf-new+hybrid and modduox-new+hybrid, which
# the README advertised and no build ever compiled; those are now MUSTFAIL
# entries in the matrix instead: the Makefile refuses HYBRID=1 on a MOD board
# with a message, rather than letting it reach the compiler.
